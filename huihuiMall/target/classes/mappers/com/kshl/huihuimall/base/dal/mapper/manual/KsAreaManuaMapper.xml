<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kshl.huihuimall.console.dal.manualmapper.KsAreaManuaMapper">

	<select id="getAllLevelOneArea" resultType="map">
        SELECT p.id, p.shortname FROM ks_area AS p where p.level = 1 AND p.status = 1
    </select>
    
</mapper>